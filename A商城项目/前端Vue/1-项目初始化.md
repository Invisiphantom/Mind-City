# 1-1 创建项目

## 步骤1: 项目创建

https://nodejs.org/en/download/package-manager


```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source .bashrc
nvm install 20

npm init vue@latest
✔ Add Vue Router for Single Page Application development?  Yes
✔ Add Pinia for state management?  Yes

cd vue-project
npm install
npm install element-plus axios pinia-plugin-persistedstate @vueuse/core dayjs
npm install -D unplugin-vue-components unplugin-auto-import sass
npm run dev
```

## 步骤2: 创建文件夹

创建~/demo.vue
```vue
<!-- demo.vue -->
<script setup>
</script>

<template>
</template>

<style scoped>
</style>
```

在vue-project文件夹下执行
```bash
cd src
rm -rf assets components
mkdir apis directives utils
cd apis && touch category.js detail.js home.js layout.js user.js && cd ..
cd directives && touch index.js && cd ..
cd stores && rm * && touch cartStore.js categoryStore.js userStore.js && cd ..
cd utils && touch bserv.js http.js && cd ..

cd views && rm -rf * && mkdir CartList Category Detail Home Layout Login SubCategory

mkdir Layout/components
cp ~/demo.vue Layout/index.vue
cp ~/demo.vue Layout/components/HeaderCart.vue
cp ~/demo.vue Layout/components/LayoutFixed.vue
cp ~/demo.vue Layout/components/LayoutFooter.vue
cp ~/demo.vue Layout/components/LayoutHeader.vue
cp ~/demo.vue Layout/components/LayoutNav.vue

mkdir Home/components
cp ~/demo.vue Home/index.vue
cp ~/demo.vue Home/components/HomeCategory.vue
cp ~/demo.vue Home/components/HomeBanner.vue
cp ~/demo.vue Home/components/HomeHot.vue
cp ~/demo.vue Home/components/HomeNew.vue
cp ~/demo.vue Home/components/HomePanel.vue
cp ~/demo.vue Home/components/HomeProduct.vue

cp ~/demo.vue Category/index.vue
cp ~/demo.vue SubCategory/index.vue
cp ~/demo.vue CartList/index.vue
cp ~/demo.vue Login/index.vue

mkdir Detail/components
cp ~/demo.vue Detail/index.vue
cp ~/demo.vue Detail/components/DetailHot.vue
```


## 步骤3: 复制静态资源

styles assets components

## main.js

```js
// main.js
import './assets/main.css'
import '@/styles/common.scss'

import { createApp } from 'vue'
import { createPinia } from 'pinia'

import App from './App.vue'
import router from './router'

const app = createApp(App)

app.use(createPinia())
app.use(router)
app.mount('#app')
```

## vite.config.js
```javascript
// vite.config.js
import { fileURLToPath, URL } from 'node:url'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      imports: ["vue", "vue-router", "pinia"],
      resolvers: [ElementPlusResolver({ importStyle: "sass" })],
    }),
    Components({
      resolvers: [
        ElementPlusResolver({ importStyle: "sass" }),
      ],
    }),
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `
          @use "@/styles/element/index.scss" as *;
          @use "@/styles/var.scss" as *;
        `,
      }
    }
  }
})
```

## router/index.js
```js
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import Login from '@/views/Login/index.vue'
import Layout from '@/views/Layout/index.vue'
import Home from '@/views/Home/index.vue'
import Category from '@/views/Category/index.vue'
import SubCategory from '@/views/SubCategory/index.vue'
import Detail from '@/views/Detail/index.vue'
import CartList from '@/views/CartList/index.vue'


const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      path: '/',
      component: Layout,
      children: [
        {
          path: '',
          component: Home
        },
        {
          path: 'category/:id',
          component: Category
        },
        {
          path: 'category/sub/:id',
          component: SubCategory
        },
        {
          path: 'detail/:id',
          component: Detail
        },
        {
          path: 'cartlist',
          component: CartList
        }
      ]
    },
    {
      path: '/login',
      component: Login
    }
  ],
  scrollBehavior() {
    return {
      top: 0
    }
  }
})

export default router
```


## App.vue
```vue
<!-- App.vue -->
<template>
  <header>
    <el-button type="primary">按钮</el-button>    
  </header>
  <RouterView />
</template>
```

# 1-2 测试接口

## utils/http.js

API文档: https://apifox.com/apidoc/shared-c05cb8d7-e591-4d9c-aff8-11065a0ec1de

```javascript
// utils/http.js
// https://apifox.com/apidoc/shared-c05cb8d7-e591-4d9c-aff8-11065a0ec1de
import axios from 'axios'
const http = axios.create({
  baseURL: 'http://pcapi-xiaotuxian-front-devtest.itheima.net',
  timeout: 15000
})

http.interceptors.request.use(config => {
  return config
}, error => Promise.reject(error))

http.interceptors.response.use(res => res.data, error => {
  return Promise.reject(error)
})

export default http
```

## apis/testAPI.js

```javascript
// apis/testAPI.js
import http from '@/utils/http'

export function getCategoryAPI () {
    return http.get('home/category/head')
}
```
## main.js

```js
// main.js
import { getCategoryAPI } from "./apis/testAPI"
getCategoryAPI().then(res=>{
  console.log(res);
})
```



# 1-3 路由设计 

Layout/index.vue
http://localhost:5173
```html
<template>
  我是首页    
</template>
```

Login/index.vue
http://localhost:5173/login
```html
<template>
  我是登录页
</template>
```

App.vue
```html
<template>
  <RouterView />
</template>
```


## 二级路由设计

Home/index.vue
http://localhost:5173
```html
<template>
  我是home
</template>
```

Category/index.vue
http://localhost:5173/category/0
```html
<template>
  我是分类
</template>
```

Layout/index.vue
```html
<template>
  我是首页
  <RouterView />
</template>
```
